To Setup:
1. Clone this repo
2. Run: `pip install -r requirements.txt`
3. Create a folder "static" at the root of this project
    and create 2 sub-folders namely "input" and "output" inside
4. Run the app: `python3 app.py`
